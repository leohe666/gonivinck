apiVersion: 1

providers:
  - name: 'SLA Dashboards'
    orgId: 1
    folder: 'SLA'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
